# ポモドーロタイマー 機能一覧

UIモックおよびアーキテクチャ設計書をもとに、実装が必要な機能を洗い出したドキュメントです。

---

## 1. タイマー機能

### 1-1. カウントダウン
- 作業セッション（デフォルト25分）のカウントダウン
- 残り時間を `MM:SS` 形式で表示
- `setInterval` による毎秒更新

### 1-2. フェーズ管理
- 作業中 / 休憩中のフェーズ切替
- 現在のフェーズ名をUI上部に表示（「作業中」「休憩中」）
- 作業セッション終了後に短い休憩（5分）へ自動遷移
- 4セッション完了後に長い休憩（15分）へ自動遷移

### 1-3. 操作ボタン
- **開始ボタン**：タイマーを開始 / 一時停止するトグル動作
- **リセットボタン**：現在フェーズのタイマーを初期値に戻す

---

## 2. 円形プログレスバー

- SVG の `stroke-dashoffset` を使った円弧アニメーション
- タイマー残り時間に応じてリアルタイムに進捗を反映
- 作業中と休憩中で円弧の色を切り替える（例：青系 / 緑系）

---

## 3. 通知機能

- タイマー完了時にブラウザ通知（Web Notifications API）を表示
- ページロード時にユーザーへ通知許可をリクエスト
- 通知非対応ブラウザや許可拒否時はUIのみで代替対応

---

## 4. 進捗記録・表示機能

### 4-1. セッション記録（バックエンド）
- 作業セッション完了時に Flask API（`POST /api/session`）へ記録を送信
- SQLite の `sessions` テーブルへ日付・完了数・集中時間を保存

### 4-2. 今日の進捗表示（フロントエンド）
- ページロード時に Flask API（`GET /api/today`）から今日のデータを取得
- 完了セッション数（例：4 完了）を表示
- 当日の合計集中時間（例：1時間40分）を表示
- セッション完了のたびにリアルタイムで数値を更新

---

## 5. Flask バックエンド

| エンドポイント | メソッド | 処理内容 |
| --- | --- | --- |
| `/` | GET | `index.html` を返す |
| `/api/session` | POST | セッション完了レコードを SQLite に挿入 |
| `/api/today` | GET | 当日の完了数・合計集中時間を集計して返す |

---

## 6. UI / スタイリング

- UIモックに沿ったレイアウト・配色の実装
  - 背景：紫系グラデーション
  - カード：白・角丸
  - プログレスバー：青紫系
  - ボタン：塗りつぶし（開始）／アウトライン（リセット）
- 今日の進捗パネル（薄いグレー背景・2カラム）
- フォント・余白はモックに準拠したシンプルなデザイン

---

## 7. 環境・設定

- `requirements.txt` に依存ライブラリ（`flask`）を記載
- SQLite データベースファイルの初期化処理（テーブルが存在しない場合に自動作成）
- Flask の開発サーバー起動設定（`debug=True`）

---

## 実装優先順位

| 優先度 | 機能 |
| --- | --- |
| 高 | タイマーカウントダウン・操作ボタン・フェーズ管理 |
| 高 | 円形プログレスバー |
| 高 | Flask 基本エンドポイント・SQLite 初期化 |
| 中 | セッション記録・今日の進捗表示 |
| 中 | UI スタイリング（モック準拠） |
| 低 | ブラウザ通知 |
